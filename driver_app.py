import streamlit as st
import pandas as pd
import datetime
import plotly.express as px

# --- 1. การตั้งค่าและเตรียมระบบ ---
st.set_page_config(page_title="ระบบบันทึกรายได้คนขับ", page_icon="🚗", layout="wide")

DATA_FILE = "driver_data.csv"

def load_data():
    try:
        df = pd.read_csv(DATA_FILE)
    except FileNotFoundError:
        df = pd.DataFrame(columns=[
            'วันที่', 'เวลา', 'แอป', 'หมวดหมู่', 'รายการ', 
            'ยอดเต็ม/หน้าแอป', 'หัก/จ่าย', 'ทิป', 'คงเหลือ/สุทธิ', 
            'ระยะทาง(กม.)', 'เลขไมล์', 'หมายเหตุ'
        ])
    return df

def save_data(df):
    df.to_csv(DATA_FILE, index=False)

if 'data' not in st.session_state:
    st.session_state.data = load_data()

# ==========================================
# 🔧 AUTO-FIX: เพิ่มคอลัมน์ 'เลขไมล์' อัตโนมัติ
# ==========================================
# ถ้าข้อมูลเก่าไม่มีคอลัมน์เลขไมล์ ให้เติมเข้าไป
if 'เลขไมล์' not in st.session_state.data.columns:
    st.session_state.data['เลขไมล์'] = 0.0
    save_data(st.session_state.data)

# แปลงชื่อคอลัมน์เก่า (ถ้ามีหลุดมา)
if 'Category' in st.session_state.data.columns:
    eng_to_thai = {
        'Date': 'วันที่', 'Time': 'เวลา', 'Platform': 'แอป',
        'Category': 'หมวดหมู่', 'SubCategory': 'รายการ',
        'Amount_Gross': 'ยอดเต็ม/หน้าแอป', 'Deduction': 'หัก/จ่าย',
        'Tip': 'ทิป', 'Net_Income': 'คงเหลือ/สุทธิ',
        'Distance_Km': 'ระยะทาง(กม.)', 'Note': 'หมายเหตุ'
    }
    st.session_state.data.rename(columns=eng_to_thai, inplace=True)

# --- 2. Sidebar ---
with st.sidebar:
    st.title("⚙️ ตั้งค่า")
    maxim_comm_rate = st.slider("Maxim หักคอมมิชชั่น (%)", 0, 30, 15) / 100
    ev_home_rate = st.number_input("ค่าไฟชาร์จบ้าน (เหมาจ่าย/ครั้ง)", value=40, step=5)
    
    st.divider()
    
    # แสดงเลขไมล์ล่าสุดเพื่อช่วยจำ
    df_odom = st.session_state.data[st.session_state.data['เลขไมล์'] > 0]
    if not df_odom.empty:
        last_odom = df_odom['เลขไมล์'].max()
        st.metric("🚗 เลขไมล์ล่าสุด", f"{last_odom:,.0f}")
    
    if st.button("⚠️ ล้างข้อมูลทั้งหมด", type="primary"):
        st.session_state.data = pd.DataFrame(columns=[
            'วันที่', 'เวลา', 'แอป', 'หมวดหมู่', 'รายการ', 
            'ยอดเต็ม/หน้าแอป', 'หัก/จ่าย', 'ทิป', 'คงเหลือ/สุทธิ', 
            'ระยะทาง(กม.)', 'เลขไมล์', 'หมายเหตุ'
        ])
        save_data(st.session_state.data)
        st.rerun()

# --- 3. Main App ---
st.title("🚗 ระบบบันทึกรายได้ (Driver Pro)")

tab1, tab2, tab3 = st.tabs(["📝 บันทึกงาน", "📊 สรุปผลกำไร", "🗂️ ฐานข้อมูล"])

# ==========================================
# TAB 1: บันทึกงาน
# ==========================================
with tab1:
    col_type, col_form = st.columns([1, 2])
    
    with col_type:
        st.subheader("ทำรายการ")
        # ย้ายการบันทึกเลขไมล์มาไว้บนสุด เพื่อให้กดง่าย
        entry_type = st.radio(
            "ประเภทรายการ",
            ["🕒 เริ่มงาน/เลิกงาน (เลขไมล์)", "🚗 รับงานขับรถ", "⛽ เติมน้ำมัน/ชาร์จไฟ", "💳 เติมเครดิตแอป", "🛠️ จ่ายอื่นๆ"],
        )

    with col_form:
        st.container(border=True)
        
        # --- NEW: บันทึกเลขไมล์ ---
        if entry_type == "🕒 เริ่มงาน/เลิกงาน (เลขไมล์)":
            st.markdown("#### 🕒 บันทึกการเข้า-ออกงาน")
            st.info("💡 ใส่เลขไมล์แค่ตอน 'เริ่มขับ' และ 'เลิกขับ' ระบบจะคำนวณระยะทางจริงให้เอง")
            
            shift_type = st.radio("สถานะ", ["☀️ เริ่มงาน (Start)", "🌙 เลิกงาน (End)"], horizontal=True)
            
            # ดึงเลขไมล์ล่าสุดมาเป็นค่าเริ่มต้นให้ (จะได้ไม่ต้องพิมพ์ใหม่หมด)
            default_odom = 0.0
            if not st.session_state.data.empty:
                max_odom = st.session_state.data['เลขไมล์'].max()
                if max_odom > 0:
                    default_odom = float(max_odom)

            odometer = st.number_input("เลขไมล์หน้าปัด (Odometer)", min_value=0.0, value=default_odom, step=1.0)
            
            if st.button("บันทึกเลขไมล์ 💾", type="primary", use_container_width=True):
                if odometer > 0:
                    action_name = "เริ่มงาน" if "เริ่ม" in shift_type else "เลิกงาน"
                    new_row = {
                        'วันที่': datetime.date.today(),
                        'เวลา': datetime.datetime.now().strftime("%H:%M"),
                        'แอป': 'ระบบ',
                        'หมวดหมู่': 'กะงาน',
                        'รายการ': action_name,
                        'ยอดเต็ม/หน้าแอป': 0, 'หัก/จ่าย': 0, 'ทิป': 0, 'คงเหลือ/สุทธิ': 0,
                        'ระยะทาง(กม.)': 0,
                        'เลขไมล์': odometer, # บันทึกเลขไมล์ตรงนี้
                        'หมายเหตุ': f"เลขไมล์ {action_name}"
                    }
                    st.session_state.data = pd.concat([st.session_state.data, pd.DataFrame([new_row])], ignore_index=True)
                    save_data(st.session_state.data)
                    st.toast(f"บันทึก {action_name} ที่เลขไมล์ {odometer:,.0f} เรียบร้อย!")

        # --- รับงาน (ตัดช่องระยะทางออก เพราะเราใช้เลขไมล์รวมแล้ว) ---
        elif entry_type == "🚗 รับงานขับรถ":
            st.markdown("#### 📝 บันทึกรายได้")
            platform = st.selectbox("เลือกแอป", ["Grab", "Bolt", "Line Man", "Maxim", "Robinhood", "Win", "งานนอก"])
            
            c1, c2 = st.columns(2)
            with c1: 
                app_price = st.number_input("ราคาหน้าแอป", min_value=0.0, step=10.0)
            with c2: 
                real_receive = st.number_input("เงินที่รับจริง (รวมทิป)", min_value=0.0, value=app_price, step=10.0)
            
            # ซ่อนช่องระยะทางไว้ (หรือใส่เป็น Optional)
            with st.expander("ใส่ระยะทางตามแอป (ไม่บังคับ - ใช้เพื่อดูเทียบ)"):
                distance = st.number_input("ระยะทางงานนี้ (กม.)", min_value=0.0, step=1.0)
            
            note = st.text_input("หมายเหตุ")
            
            if st.button("บันทึกรายได้ ✅", type="primary", use_container_width=True):
                if app_price > 0:
                    deduction = 0
                    tip = max(0, real_receive - app_price)
                    if platform in ["Maxim", "งานนอก"]:
                        deduction = app_price * maxim_comm_rate
                        net_income = app_price - deduction + tip
                    else:
                        net_income = app_price + tip 

                    new_row = {
                        'วันที่': datetime.date.today(),
                        'เวลา': datetime.datetime.now().strftime("%H:%M"),
                        'แอป': platform,
                        'หมวดหมู่': 'รายรับ',
                        'รายการ': 'ค่าโดยสาร',
                        'ยอดเต็ม/หน้าแอป': app_price,
                        'หัก/จ่าย': deduction,
                        'ทิป': tip,
                        'คงเหลือ/สุทธิ': net_income,
                        'ระยะทาง(กม.)': distance,
                        'เลขไมล์': 0,
                        'หมายเหตุ': note
                    }
                    st.session_state.data = pd.concat([st.session_state.data, pd.DataFrame([new_row])], ignore_index=True)
                    save_data(st.session_state.data)
                    st.toast(f"บันทึกสำเร็จ! เข้ากระเป๋า {net_income:.0f} บาท")

        # --- พลังงาน ---
        elif entry_type == "⛽ เติมน้ำมัน/ชาร์จไฟ":
            st.markdown("#### ⚡ บันทึกต้นทุนพลังงาน")
            e_type = st.radio("ประเภท", ["⛽ น้ำมัน", "⚡ ชาร์จบ้าน (เหมา)", "🔌 ชาร์จสถานี"], horizontal=True)
            cost = st.number_input("บาท", value=(ev_home_rate if "บ้าน" in e_type else 0.0))
            note = st.text_input("สถานที่")
            
            if st.button("บันทึก", type="primary", use_container_width=True):
                new_row = {
                    'วันที่': datetime.date.today(),
                    'เวลา': datetime.datetime.now().strftime("%H:%M"),
                    'แอป': 'ค่าใช้จ่าย',
                    'หมวดหมู่': 'รายจ่าย',
                    'รายการ': 'ค่าน้ำมัน/ไฟ',
                    'ยอดเต็ม/หน้าแอป': 0, 'หัก/จ่าย': cost, 'ทิป': 0, 'คงเหลือ/สุทธิ': -cost,
                    'ระยะทาง(กม.)': 0, 'เลขไมล์': 0,
                    'หมายเหตุ': f"{e_type} - {note}"
                }
                st.session_state.data = pd.concat([st.session_state.data, pd.DataFrame([new_row])], ignore_index=True)
                save_data(st.session_state.data)
                st.toast("บันทึกเรียบร้อย")

        # --- เติมเครดิต ---
        elif entry_type == "💳 เติมเครดิตแอป":
            st.markdown("#### 💳 เติมเงินเข้ากระเป๋าแอป")
            platform = st.selectbox("เติมแอปไหน?", ["Grab Wallet", "Bolt Balance", "Maxim", "Line Man"])
            amount = st.number_input("จำนวนเงิน", min_value=0.0, step=100.0)
            if st.button("บันทึก", type="primary", use_container_width=True):
                new_row = {
                    'วันที่': datetime.date.today(),
                    'เวลา': datetime.datetime.now().strftime("%H:%M"),
                    'แอป': platform,
                    'หมวดหมู่': 'รายจ่าย',
                    'รายการ': 'เติมเครดิต',
                    'ยอดเต็ม/หน้าแอป': 0, 'หัก/จ่าย': amount, 'ทิป': 0, 'คงเหลือ/สุทธิ': -amount,
                    'ระยะทาง(กม.)': 0, 'เลขไมล์': 0, 'หมายเหตุ': "เติมเงิน"
                }
                st.session_state.data = pd.concat([st.session_state.data, pd.DataFrame([new_row])], ignore_index=True)
                save_data(st.session_state.data)
                st.toast("บันทึกเรียบร้อย")

        # --- จ่ายอื่นๆ ---
        elif entry_type == "🛠️ จ่ายอื่นๆ":
             st.markdown("#### 🛠️ ค่าใช้จ่ายทั่วไป")
             cost = st.number_input("จำนวนเงิน", min_value=0.0)
             note = st.text_input("รายการ")
             if st.button("บันทึก", type="primary", use_container_width=True):
                 new_row = {
                        'วันที่': datetime.date.today(),
                        'เวลา': datetime.datetime.now().strftime("%H:%M"),
                        'แอป': 'ค่าใช้จ่าย',
                        'หมวดหมู่': 'รายจ่าย',
                        'รายการ': 'ทั่วไป',
                        'ยอดเต็ม/หน้าแอป': 0, 'หัก/จ่าย': cost, 'ทิป': 0, 'คงเหลือ/สุทธิ': -cost,
                        'ระยะทาง(กม.)': 0, 'เลขไมล์': 0, 'หมายเหตุ': note
                    }
                 st.session_state.data = pd.concat([st.session_state.data, pd.DataFrame([new_row])], ignore_index=True)
                 save_data(st.session_state.data)
                 st.toast("บันทึกเรียบร้อย")

# ==========================================
# TAB 2: สรุปผล
# ==========================================
with tab2:
    df = st.session_state.data
    if not df.empty:
        # --- LOGIC คำนวณระยะทางจากเลขไมล์ (NEW!) ---
        # 1. กรองเฉพาะแถวที่มีเลขไมล์
        odom_df = df[df['เลขไมล์'] > 0]
        
        # 2. หาเลขไมล์สูงสุดและต่ำสุดของ 'แต่ละวัน'
        daily_dist = 0
        if not odom_df.empty:
            # Group by Date แล้วหา Max - Min
            daily_odom = odom_df.groupby('วันที่')['เลขไมล์'].agg(['min', 'max'])
            daily_odom['run_dist'] = daily_odom['max'] - daily_odom['min']
            daily_dist = daily_odom['run_dist'].sum()
        
        # ถ้าไม่มีการจดเลขไมล์ ให้ใช้ระยะทางรวมจากงาน (Fallback)
        manual_dist = df['ระยะทาง(กม.)'].sum()
        
        # ใช้ค่าไหนดี? ถ้ามีเลขไมล์ใช้เลขไมล์ ถ้าไม่มีใช้แบบเดิม
        total_km = daily_dist if daily_dist > 0 else manual_dist
        dist_source = "คำนวณจากเลขไมล์จริง" if daily_dist > 0 else "ผลรวมระยะทางแต่ละงาน"

        # --- คำนวณการเงิน ---
        income_df = df[df['หมวดหมู่'] == 'รายรับ']
        expense_df = df[df['หมวดหมู่'] == 'รายจ่าย']
        
        total_income = income_df['คงเหลือ/สุทธิ'].sum()
        fuel_cost = expense_df[expense_df['รายการ'] == 'ค่าน้ำมัน/ไฟ']['หัก/จ่าย'].sum()
        other_cost = expense_df[expense_df['รายการ'] == 'ทั่วไป']['หัก/จ่าย'].sum()
        
        net_profit = total_income - fuel_cost - other_cost

        # --- แสดงผล ---
        m1, m2, m3, m4 = st.columns(4)
        m1.metric("💰 กำไรสุทธิ", f"{net_profit:,.0f} บ.")
        m2.metric("⛽ ค่าน้ำมัน/ไฟ", f"{fuel_cost:,.0f} บ.")
        m3.metric("🛣️ ระยะทางวิ่งจริง", f"{total_km:,.0f} กม.", help=dist_source)
        
        if total_km > 0:
            cost_km = fuel_cost / total_km
            income_km = total_income / total_km
            m4.metric("📊 รายได้ vs ต้นทุน /กม.", f"{income_km:.1f} บ.", f"ต้นทุน {cost_km:.1f} บ./กม.")
        else:
            m4.metric("📊 ต้นทุน/กม.", "รอข้อมูลไมล์")

        st.divider()
        
        # กราฟ
        c1, c2 = st.columns(2)
        with c1:
            if not income_df.empty:
                plat_sum = income_df.groupby('แอป')['คงเหลือ/สุทธิ'].sum().reset_index()
                fig = px.bar(plat_sum, x='แอป', y='คงเหลือ/สุทธิ', color='แอป', text_auto=True, title="รายได้แยกตามแอป")
                st.plotly_chart(fig, use_container_width=True)
        with c2:
            if not income_df.empty:
                income_df['Hour'] = pd.to_datetime(income_df['เวลา'], format='%H:%M').dt.hour
                fig = px.histogram(income_df, x='Hour', y='คงเหลือ/สุทธิ', nbins=24, title="ช่วงเวลาทำเงิน", color_discrete_sequence=['#FF4B4B'])
                st.plotly_chart(fig, use_container_width=True)
                
    else:
        st.info("เริ่มบันทึกข้อมูลได้เลยครับ")

# ==========================================
# TAB 3: ฐานข้อมูล
# ==========================================
with tab3:
    st.subheader("🗂️ ฐานข้อมูล")
    if not st.session_state.data.empty:
        edited_df = st.data_editor(st.session_state.data, num_rows="dynamic", use_container_width=True)
        if st.button("💾 บันทึกการแก้ไข", type="primary"):
            st.session_state.data = edited_df
            save_data(edited_df)
            st.success("บันทึกแล้ว")
